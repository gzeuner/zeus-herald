/**
 * zeus-herald entrypoint.
 * Package 00-foundation: placeholder until notifier-core and runtime land.
 */

const version = '0.1.0';

console.log(`zeus-herald ${version} – foundation scaffold ready`);
console.log('Configure TELEGRAM_* and NTFY_URL via .env (see .env.example).');

export { version };
